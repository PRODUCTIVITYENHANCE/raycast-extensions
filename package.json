{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "add-md-to-folder",
  "title": "Add Md to Folder",
  "description": "Create Markdown file from clipboard to specified folder",
  "icon": "extension-icon.png",
  "author": "Panpan",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "preferences": [
    {
      "name": "rootDirectory",
      "title": "Root Directory",
      "description": "Root directory for Markdown files",
      "type": "directory",
      "required": true,
      "default": "~/Desktop"
    },
    {
      "name": "defaultSubfolder",
      "title": "Default Subfolder",
      "description": "Default subfolder (leave empty to save to root)",
      "type": "textfield",
      "required": false,
      "default": ""
    },
    {
      "name": "defaultEditor",
      "title": "Default Editor",
      "description": "Default application to open Markdown files (e.g. Visual Studio Code, Obsidian, Typora)",
      "type": "textfield",
      "required": false,
      "default": "Visual Studio Code"
    }
  ],
  "commands": [
    {
      "name": "add-md-to-folder",
      "title": "Add Md to Folder",
      "description": "Create Markdown file from clipboard (use default folder)",
      "mode": "no-view"
    },
    {
      "name": "add-md-select-folder",
      "title": "Add Md (Select Folder)",
      "description": "Create Markdown file from clipboard (select subfolder)",
      "mode": "view"
    },
    {
      "name": "browse-md-files",
      "title": "Browse Markdown Files",
      "description": "Browse all Markdown files in the root directory",
      "mode": "view"
    },
    {
      "name": "append-to-md",
      "title": "Append to Markdown",
      "description": "Append text to existing Markdown files",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.1",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}